# FROM ## Base Image

# WORKDIR ## Docker working directory

# COPY ./requirements.txt ./requirements.txt

# RUN pip install requirements.txt

# COPY ./src ./src

# CMD [## Uvicorn ./src/main:app 80:80]